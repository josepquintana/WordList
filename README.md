# WordList
Generate word lists with all the possible combinations with length 'n'

Usage: java -jar WordList.jar

    -h, --help
    -n, --number 'NUM'
    -f, --file '/path/to/file'
    -v, --verbose
